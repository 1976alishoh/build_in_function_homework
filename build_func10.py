def main(x,y):
    result = 3*(y**(1/2)) + x**(2/3)
    """
    Given a arguments called 'x' and 'y' type of int , calculate the value of expression and round the result to 2 decimal places, use the pow function use the round function ,return result:
    Args:
        x: int
        y: int
    Returns:
        result : float
    """
    return result
print(main(8,4))

